import React from "react";
import { FormEvent, useState } from "react";
import { useMultistepForm } from "../UseMultiForm";

type Props = {}

type FormData = {
    firstName: string;
    
};

export default function PatientRegisPage({}: Props) {
  return (
    <div>PatientRegisPage</div>
  )
}